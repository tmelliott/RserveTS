# Check if debug logging is enabled for a tag

Check if debug logging is enabled for a tag

## Usage

``` r
rts_debug_enabled(tag = "general")
```

## Arguments

- tag:

  Character tag to check

## Value

Logical
