# Self object

Representation of the 'self' property, used by recursive list
definitions

## Usage

``` r
ts_self(n = -1L)
```

## Arguments

- n:

  number of elements, either n = 1 (for singular) or n != 0 (for array)

## Value

a representation of 'self'
