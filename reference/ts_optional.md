# Optional type

A wrapper around union of a type and undefined

## Usage

``` r
ts_optional(type)
```

## Arguments

- type:

  Type that is optional

## Examples

``` r
x <- ts_optional(ts_numeric(1))
```
