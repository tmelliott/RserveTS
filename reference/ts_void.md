# Void type

This is a type that accepts null values (this would typically be used
for functions that return nothing).

## Usage

``` r
ts_void()
```

## Value

A ts object that accepts `NULL`.

## See also

[`ts_null`](http://tomelliott.co.nz/RserveTS/reference/ts_null.md)
