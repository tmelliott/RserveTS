% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/widgets.R
\name{observer}
\alias{observer}
\title{Create an Observer for Reactive Methods}
\usage{
observer(props, fn)
}
\arguments{
\item{props}{Character vector of property names to observe.}

\item{fn}{The method body: a plain \code{function} (internal) or a
\code{ts_function} (exported to JS).}
}
\value{
A \code{ts_observer} object used by \code{createWidget()}.
}
\description{
Wraps a method so it reacts to property changes. Used inside the
\code{methods} list of \code{createWidget()} to declare which properties
trigger the method.
}
\examples{
\dontrun{
createWidget("Example",
    properties = list(x = ts_integer(1L, default = 0L)),
    methods = list(
        on_x = observer("x", function() {
            cat("x changed to", .self$x, "\n")
        })
    )
)
}
}
