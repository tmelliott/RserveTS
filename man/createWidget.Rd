% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/docs_widgets.R, R/widgets.R
\docType{class}
\name{createWidget}
\alias{createWidget}
\alias{widgetActions}
\alias{observer}
\alias{tsWidget-class}
\alias{tsWidget}
\title{Create 'TypeScript'-compatible widgets}
\usage{
widgetActions(..., strict = "warn", enabled = TRUE)

createWidget(
  name,
  properties = list(),
  initialize = NULL,
  methods = list(),
  actions = FALSE,
  auto_flush = TRUE,
  .env = parent.frame(),
  ...
)

observer(props, fn)
}
\arguments{
\item{...}{For \code{createWidget()}: passed to the underlying \code{ts_function()}
constructor. For \code{widgetActions()}: named \code{ts_function()} action definitions.}

\item{strict}{For \code{widgetActions()}: unknown action handling (\code{"off"},
\code{"warn"}, or \code{"strict"}).}

\item{enabled}{For \code{widgetActions()}: whether action support is enabled.}

\item{name}{Widget class name (character).}

\item{properties}{Named list of typed properties (\verb{ts_*()} objects, or nested
widget constructors).}

\item{initialize}{Optional function run after defaults are applied; receives
the widget instance when it has a parameter.}

\item{methods}{Named list of \code{ts_function()} methods and/or \code{observer()}
reactive methods.}

\item{actions}{\code{FALSE}/\code{TRUE}, a list with \code{enabled}/\code{types}/\code{strict}, or a
\code{widgetActions()} object.}

\item{auto_flush}{If \code{TRUE} (default), methods flush state to 'TypeScript'
after they return; if \code{FALSE}, call \code{updateState()} manually.}

\item{.env}{Environment for the ref class definition (default
\code{parent.frame()}).}

\item{props}{For \code{observer()}: property names that trigger the method.}

\item{fn}{For \code{observer()}: method body (\code{function} or \code{ts_function()}).}
}
\value{
\code{createWidget()} returns a \code{ts_widget} constructor.
\code{widgetActions()} returns a \code{ts_widget_actions} object.
\code{observer()} returns a \code{ts_observer} object.
\code{tsWidget} is the base reference class generator.
}
\description{
Widgets are stateful reference classes shared between R and 'TypeScript'.
Use \code{createWidget()} to define one, wrap reactive methods with \code{observer()},
and optionally declare typed actions with \code{widgetActions()}. Instances
inherit from the \code{tsWidget} reference class.
}
\details{
\code{createWidget()} returns a \code{ts_function()}-like constructor (class
\code{ts_widget}) that 'JavaScript' calls with a state setter. Locally you can
inspect or compile it with \code{\link[=ts_compile]{ts_compile()}} without a live 'Rserve' session;
calling \verb{$call()} needs an out-of-band 'JavaScript' setter.
\subsection{Properties and methods}{

Each entry in \code{properties} is a \verb{ts_*()} type (optionally with \code{default}).
Child widgets can be nested by passing another \code{createWidget()} result as a
property value.

Each entry in \code{methods} is usually a \code{ts_function()} exported to
'JavaScript'. Use \code{observer()} to run a method when properties change
(internal-only if the body is a plain \code{function}).
}

\subsection{Actions}{

Pass \code{actions = widgetActions(...)} to enable typed, named actions
dispatched from 'JavaScript'. Each action must be a named \code{ts_function()}
with exactly one payload argument. \code{strict} controls unknown action handling
(\code{"off"}, \code{"warn"}, or \code{"strict"}).
}

\subsection{\code{tsWidget} reference class}{

All widget instances inherit \code{tsWidget} and include:
\itemize{
\item \code{set(prop, value)}, \code{get(prop)} -- field access with change tracking
\item \code{updateState(all = FALSE)} -- push changed properties to 'TypeScript'
\item \code{addPropHandler(prop, fn)} -- react to property changes
\item \code{batch(props, expr)} -- batch updates into one state flush
\item \code{add_child(property, widget_def)} -- attach a nested widget
\item \code{create_dynamic_child(widget_def)} -- runtime child widget
\item \code{destroy()} -- tear down the widget
}
}

\subsection{Client apps ('rserve-ts' / React)}{

Compile widgets with \code{\link[=ts_compile]{ts_compile()}} and import the generated schema into a
client app that connects to 'Rserve' via the \href{https://www.npmjs.com/package/rserve-ts}{'rserve-ts'}
library. Obtain widget Ocaps from the compiled app schema (for example
\code{app.histogram} after connecting with \code{useRserve()} in React).

In React, \href{https://www.npmjs.com/package/@tmelliott/react-rserve}{\code{useWidget()}}
from \verb{@tmelliott/react-rserve} wraps a compiled widget constructor and keeps
'JavaScript' state in sync with R:
\itemize{
\item \code{state} -- current property values (from R \code{updateState()})
\item \code{set} -- update properties from the client
\item \code{methods} -- call exported \code{ts_function()} methods on the widget
\item \code{children} -- nested widget connectors when properties include child widgets
}

Action-enabled widgets (\code{actions = widgetActions(...)}) also expose
\code{capabilities}, \code{dispatchAction}, \code{undo}, and \code{redo} on the hook return value.
}
}

\examples{
Counter <- createWidget(
    name = "Counter",
    properties = list(count = ts_integer(1L, default = 0L)),
    methods = list(
        increment = ts_function(function(by = ts_integer(1L)) {
            .self$count <- as.integer(.self$count + by)
            .self$count
        }, result = ts_integer(1)),
        on_count = observer("count", function() NULL)
    )
)
inherits(Counter, "ts_widget")
ts_compile(Counter)

}
\seealso{
\code{\link[=ts_function]{ts_function()}}, \code{\link[=ts_compile]{ts_compile()}}, \link{type_objects},
\href{https://www.npmjs.com/package/@tmelliott/react-rserve}{Package '@tmelliott/react-rserve'}
}
